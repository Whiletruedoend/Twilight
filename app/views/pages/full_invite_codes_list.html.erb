<body id="bg">
  <% if current_user.present? && current_user.is_admin? %>
    <div class="comments-container">
      <%= render "users/registrations/manage/invite_codes" %><br>
      <center><%= link_to I18n.t("posts.back"), edit_user_path %> | <%= link_to I18n.t("posts.main_page"), root_path %></center>
    </div>
  <% else %>
    <center><%=I18n.t("manage.manage_admins_only")%></center>
  <% end %>
</body>